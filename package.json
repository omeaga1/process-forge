{
  "name": "process-forge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Next-generation hybrid process simulation platform with hierarchical agent orchestration",
  "author": "omeaga1",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.5.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "verify:scaffolds": "node ./packages/scaffold-registry/dist/cli.js check",
    "mcp:start": "pnpm --filter @process-forge/mcp-server start",
    "deploy:web": "pnpm --filter @process-forge/web build && wrangler pages deploy apps/web/dist --project-name process-forge",
    "deploy:api": "pnpm --filter @process-forge/community-library-api deploy",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  }
}
